# GitHub Integration Workflow

1) Create account
2) Create project
3) Create code vault with `source_type: GIT` and `source_url`, `username`, `password` (this could be a Personal Acccess Token) and optionally a `branch`
4) Poll `get-code-vault-summary` and `get-code-vault-results`
5) Fetch `get-code-vault-reports`
