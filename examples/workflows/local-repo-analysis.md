# Local Repo Analysis Workflow

1) Create account
2) Create project
3) Create code vault with `source_type: LOCAL_AGENT`
4) Run `next_steps.commands` locally
5) Poll `get-code-vault-results` until ready
6) Fetch `get-code-vault-reports`
